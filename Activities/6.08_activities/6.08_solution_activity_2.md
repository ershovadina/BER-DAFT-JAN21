```VBA
Sub variables_macro()

' Declare a string Variable
Dim eid As String

' Give it a Value
eid = "EMP002"

Dim name As String
name = "John Doe"

Dim age As String
age = "60"

Dim email As String
email = "john.doe@ironhack.com"

Dim dob As Date
dob = Format("4/22/1992", "Short Date")
' dob = Format("4/22/1992", "Long Date")
' dob = Format("4/22/1992", "yyyy/mm/dd")

Dim married As Boolean
married = True

Dim location As String
location = "Orlando"

' Now we can enter these values
Range("A3").Value = eid
Range("A3").Offset(0, 1).Select
Selection.Value = name

Selection.Offset(0, 1).Select
Selection.Value = age

Selection.Offset(0, 1).Select
Selection.Value = email

Selection.Offset(0, 1).Select
Selection.Value = dob

Selection.Offset(0, 1).Select
Selection.Value = married

Selection.Offset(0, 1).Select
Selection.Value = location

ActiveSheet.Cells.EntireColumn.AutoFit

End Sub
```